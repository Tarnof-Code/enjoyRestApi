<!-- Fiche Memory Bank — point d'entrée : [AI_MEMORY.md](../../AI_MEMORY.md) -->

## Contexte Actif
- **Phase** : Refactoring Architectural & Tests
- **Focus Actuel** : Migration vers les standards de code (Constructor Injection ✅, Java Records ✅) et ajout de tests unitaires ; compléter la couverture (dossier enfant, activités, **moments**, **activités prestataires** ; **`TypeActiviteControllerTest`** si besoin).
- **Dernière mise à jour** : 2026-06-07
- **Dernières modifications** :
  - **Moments — hiérarchie parent/enfants** : Entité **`Moment`** auto-référente : **`parent`** (**`@ManyToOne`** nullable, **`parent_id`**) + **`enfants`** (**`@OneToMany`**). **Profondeur libre**, **un seul parent**, plusieurs enfants. **`MomentDto.parentId`** et **`SaveMomentRequest.parentId`** (création + modification : rattacher/détacher/regrouper). Validation **`MomentServiceImpl.resoudreParent`** : parent du **même séjour** + **anti-cycle** (`verifierAbsenceDeCycle`). **Suppression** d’un parent **bloquée** s’il a des sous-moments (**`MomentRepository.existsByParentId`**, **400**), **pas** de cascade. *Migration* : **`parent_id`** créée par **`ddl-auto: update`** en dev, nullable, aucun backfill. Contrats : [documentation-api-rest.md](./documentation-api-rest.md) ; modèle : [etat-projet.md](./etat-projet.md). ⚠️ Front : `MomentDto.parentId`, reconstruction de l’arbre depuis la liste plate.
  - **Activité — conflit animateur étendu à la hiérarchie des moments** : Un même membre ne peut plus encadrer deux activités le **même jour** sur des moments qui **se chevauchent** (même moment, ou l’un **ancêtre/descendant** de l’autre, profondeur complète). **`ActiviteServiceImpl.momentsEnConflit`** calcule `{moment} ∪ {ancêtres} ∪ {descendants}` ; nouvelle requête **`ActiviteRepository.findMomentsEnConflitPourMembre(sejourId, date, momentIds, utilisateurId, excludeActiviteId)`** (remplace **`countActivitesAvecMembreMemeCreneau`**). Refus **bloquant** **`ConflitPlanningAnimateurException`** (**400**, code **`ANIMATEUR_DEJA_AFFECTE_CRENEAU`**), message citant le **moment réellement occupé**. **Périmètre : activités uniquement** (planning organisation non concerné, aucune détection de conflit animateur n’y existe). **Aucune migration BDD**. Tests **`ActiviteServiceImplTest`** réalignés. Contrats : [documentation-api-rest.md](./documentation-api-rest.md).
  - **Chambres — historique des modifications** : Sous-classe **`HistoriqueModificationChambre`** (discriminant **`CHAMBRE`**, colonne **`chambre_id`**, index **`idx_hist_mod_chambre`**). **`HistoriqueModificationType.CHAMBRE`**, DTO **`HistoriqueModificationChambreDto`** (`@JsonUnwrapped` → champs à plat). **`HistoriqueModificationService.enregistrerChambre`** / **`listerHistoriqueChambre`**. **`ChambreServiceImpl`** enregistre automatiquement **CREATION** / **MODIFICATION** / **SUPPRESSION** (CRUD chambre, référents, affectation/retrait occupants) via **`libelleChambrePourHistorique`** + détection **`signatureTechniqueChambre`**. Endpoint **`GET /api/v1/sejours/{sejourId}/chambres/{chambreId}/historique`** (**`ACCES_SEJOUR`** + appartenance). **`ChambreController`** injecte **`HistoriqueModificationService`**. Contrats : [documentation-api-rest.md](./documentation-api-rest.md) ; modèle : [etat-projet.md](./etat-projet.md). ⚠️ Front : **`HistoriqueModificationChambreDto`** + écran historique (même UX que activités / cahier d’infirmerie).
  - **Chambres — affectation des occupants** : Entité **`ChambreOccupant`** (table **`chambre_occupant`**) : lien **`Chambre`** ↔ **`Enfant`** ou **`Utilisateur`** (membre d’équipe), **`numeroLit`** optionnel. Contraintes **`uk_chambre_occupant_enfant`** / **`uk_chambre_occupant_utilisateur`** : **un enfant** et **un membre** au plus **par séjour**. Réaffectation vers une autre chambre = déplacement (suppression de l’ancienne ligne). **`Chambre.groupe`** (**`ManyToOne`**, chambres **`ENFANT`** only) : si renseigné, seuls les enfants du groupe peuvent y dormir ; **`SaveChambreRequest.groupeId?`**. **`ChambreGenreRules`** : compatibilité **`Genre`** ↔ **`GenreChambre`** (`MIXTE` accepte tous). Endpoints **`GESTION_SEJOURS`** : **`POST .../occupants/enfants`** (batch **`AffecterOccupantsEnfantsRequest`**), **`POST .../occupants/enfants/{enfantId}`** (body optionnel **`AffecterOccupantChambreRequest`**), **`DELETE .../occupants/enfants/{enfantId}`** ; idem **`/occupants/equipe`** avec **`membreTokenId`**. Règles : capacité, lit unique par chambre (**409**), genre, inscription enfant / appartenance membre au séjour. **`ChambreDto`** enrichi : **`groupe`** (`GroupeResumeDto`), **`occupants[]`** (`ChambreOccupantDto`). **PUT** chambre : changement de **`typeChambre`** efface les occupants ; vérif genre/groupe/capacité vs occupants restants. **`ChambreRepository`** : fetch occupants + requête séparée référents (évite **`MultipleBagFetchException`**). Tests : **`ChambreControllerTest`** (**12**), **`ChambreServiceImplTest`** (**26**). Contrats : [documentation-api-rest.md](./documentation-api-rest.md) ; modèle : [etat-projet.md](./etat-projet.md). ⚠️ Aligner **`api.d.ts`** (front).
  - **Chambres (hébergement séjour)** : Ressource **`/api/v1/sejours/{sejourId}/chambres`**. Entité **`Chambre`** (**`ManyToOne`** **`Sejour`**, **`TypeChambre`** : **`ENFANT`** / **`EQUIPE`**, **`identifiant`** obligatoire unique par séjour — casse ignorée, **`uk_chambre_sejour_identifiant`**, **`nom`** optionnel surnom, **`capaciteMax`**, **`GenreChambre`**, localisation **`batiment` / `couloir` / `etage`**, **`description`**). **`ENFANT`** : **`@ManyToMany`** référents (**`chambre_referent`**, pattern groupes) ; **`EQUIPE`** : pas de référents (**400** si ajout ; passage **`ENFANT` → `EQUIPE`** en PUT efface les référents). **`Sejour.chambres`** **`OneToMany`** ; **`Utilisateur.chambresReferent`**. DTOs : **`ChambreDto`**, **`SaveChambreRequest`**, réutilisation **`AjouterReferentRequest`** (`referentTokenId`). **Lecture** **`ACCES_SEJOUR`** + appartenance ; **écriture** **`GESTION_SEJOURS`**. Tri liste : bâtiment, étage, couloir, identifiant. Voir puce **affectation occupants** ci-dessus pour le détail API. Contrats : [documentation-api-rest.md](./documentation-api-rest.md) ; modèle : [etat-projet.md](./etat-projet.md).
  - **Activités prestataires — historique des modifications** : Sous-classe **`HistoriqueModificationActivitePrestataire`** (discriminant **`ACTIVITE_PRESTATAIRE`**, colonne **`activite_prestataire_id`**, index **`idx_hist_mod_ap`**). **`HistoriqueModificationType.ACTIVITE_PRESTATAIRE`**, DTO **`HistoriqueModificationActivitePrestataireDto`**. **`HistoriqueModificationService.enregistrerActivitePrestataire`** / **`listerHistoriqueActivitePrestataire`**. **`ActivitePrestataireServiceImpl`** capture **CREATION** / **MODIFICATION** / **SUPPRESSION** via **`libelleActivitePrestatairePourHistorique`** + **`signatureTechniqueActivitePrestataire`** (inclut **`nonParticipations`**). Endpoint **`GET .../activites-prestataires/{activitePrestataireId}/historique`** (**`ACCES_SEJOUR`**). **`ActivitePrestataireController`** injecte **`HistoriqueModificationService`** ; POST/PUT/DELETE passent **`modificateurTokenId`**. Tests : **`ActivitePrestataireServiceImplTest`**, **`ActivitePrestataireControllerTest`**. ⚠️ Front : **`HistoriqueModificationActivitePrestataireDto`**.
  - **Activités prestataires (sorties / prestas)** : Ressource **`/api/v1/sejours/{sejourId}/activites-prestataires`**. Entité **`ActivitePrestataire`** (**`ManyToOne`** **`Sejour`**, **`LocalDate` date**, **`nom`**, **`heureDepart` / `heureRetour`** optionnels **`HH:mm`**, **`informations`**, **`telephone`**, **`@ManyToMany`** **`Moment`** via **`activite_prestataire_moment`**, **`@ManyToMany`** **`Groupe`** via **`activite_prestataire_groupe`**). **`momentIds`** min. 1 en entrée ; **`moments[]`** tri chronologique en sortie. **Non-participations calendrier** : table **`activite_prestataire_non_participation`** (**`uk_ap_non_participation`** sur sortie + utilisateur + moment) ; DTO **`NonParticipationPrestataireDto`** (`tokenId`, `momentId`) dans **`ActivitePrestataireDto.nonParticipations`** et **`SaveActivitePrestataireRequest`** (liste complète de remplacement si fournie ; **`null`** en PUT → conserver + élaguer). Animateurs concernés = **référents** des **`groupeIds`**. **PUT** : sync incrémentale des non-participations (réutilise les lignes existantes — correctif doublon **`uk_ap_non_participation`**). **Anti-doublon sorties** : interdit deux sorties même **`date` + moment + groupe`** (**`ActivitePrestataireRepository.countAutreSortieMemeDateMomentGroupe`**, **400**). **Lecture** **`ACCES_SEJOUR`** ; **écriture** **`GESTION_SEJOURS`**. Suppression **`Moment`** : aussi bloquée si sorties rattachées (**`ActivitePrestataireRepository.existsByMoments_Id`**). Voir puce **historique** ci-dessus. Tests : **`ActivitePrestataireServiceImplTest`**, **`ActivitePrestataireControllerTest`**. Contrats : [documentation-api-rest.md](./documentation-api-rest.md) ; modèle : [etat-projet.md](./etat-projet.md).
  - **Utilisateurs — restriction modification email (`PUT /api/v1/utilisateurs`)** : Un directeur ne peut pas modifier son propre email (réservé à l'admin) ; un membre `BASIC_USER` ne peut pas modifier le sien (réservé à un directeur ou à l'admin). Nouveau chemin service **`modifUserByDirector`** ; routage dans **`UtilisateurController`**. Refus **`403`** si changement d'email sans droit. Contrat : [documentation-api-rest.md](./documentation-api-rest.md) ; décision : [decisions-architecturales.md](./decisions-architecturales.md) ; tests : **`UtilisateurServiceImplTest`**, **`UtilisateurControllerTest`**.
  - **Réunions / comptes rendus (séjour)** : Ressource **`/api/v1/sejours/{sejourId}/reunions`**. Entité **`Reunion`** (**`ManyToOne`** **`Sejour`**, **`dateReunion`** obligatoire, **`ordreDuJour`** optionnel jusqu’à **500** car., **`contenuJson`** TEXT document TipTap). **`Sejour.reunions`** **`OneToMany`** cascade / orphanRemoval. **`ReunionRepository.findBySejour_IdOrderByDateReunionAscIdAsc`**. **Lecture** : **`ACCES_SEJOUR`** + appartenance ; **écriture** : **`GESTION_SEJOURS`**. DTOs records **`ReunionDto`**, **`SaveReunionRequest`** (`date`, `ordreDuJour?`, **`contenu`** `JsonNode`). Tests : **`ReunionControllerTest`**, **`ReunionServiceImplTest`**. Contrats détaillés : [documentation-api-rest.md](./documentation-api-rest.md) ; sync types : [etat-projet.md](./etat-projet.md).
  - **Cahier d’infirmerie (traçabilité + droits)** : Ressource séjour **`/api/v1/sejours/{sejourId}/cahier-infirmerie`**. Entité **`CahierInfirmerieEntree`** (`sejour`, `enfant`, **`createur`**, **`soigneur`** `ManyToOne` **`Utilisateur` obligatoire**, même règle équipe que les animateurs d’activité, `dateHeure` **`Instant`**, description, localisation corps, **`Set<TypeSoinInfirmerie>`** / **`Set<TypeAppelInfirmerie>`**, précisions « autre »). **POST** : le **créateur** = connecté ; **`soigneurTokenId`** = autre (ou soi-même) parmi directeur / **`sejour_equipe`**. **PUT** / **DELETE** : gestion complète du séjour (**`aDroitGestionCompleteSurSejour`**) ou **auteur** ou **soigneur** (**`verifierDroitModificationEntreeCahierInfirmerie`** / **`verifierDroitSuppressionEntreeCahierInfirmerie`**, **ADMIN** illimité). **`DELETE`** : **`ACCES_SEJOUR`** au contrôleur + vérif service. Historique **`HistoriqueModificationCahierInfirmerie`**. Détail contrat : [documentation-api-rest.md](./documentation-api-rest.md).
  - **Planning — PATCH « ma présence » sur cellules `MEMBRE_EQUIPE`** : **`PATCH .../planning-grilles/{grilleId}/lignes/{ligneId}/cellules/{jour}/ma-presence`**, **`ACCES_SEJOUR`** + appartenance ; **`ModifierMaPresenceCelluleMembreEquipeRequest.present`** ; **`PlanningGrilleServiceImpl.modifierMaPresenceSurCelluleMembreEquipe`** (ajout/retrait **uniquement du connecté**, contrôle type de contenu + **`chargerMembresCelluleValides`**) ; historique cellule aligné (**`CREATION`** / **`MODIFICATION`** / **`SUPPRESSION`**) ; **`PUT .../cellules`** reste **`GESTION_SEJOURS`**. Contrats : [documentation-api-rest.md](./documentation-api-rest.md) ; tests **`PlanningGrilleControllerTest`**, **`PlanningGrilleServiceImplTest`**.
  - **Historique des modifications (Activités, Planning, Cahier d’infirmerie, Chambres & sorties prestataires) avec capture automatique des valeurs** : Système de traçabilité des créations, modifications et suppressions. Entité **`HistoriqueModification`** avec stratégie **SINGLE_TABLE** (`type` discriminant) et sous-classes par domaine (**`HistoriqueModificationActivite`**, **`HistoriqueModificationPlanningCellule`**, **`HistoriqueModificationCahierInfirmerie`**, **`HistoriqueModificationChambre`**, **`HistoriqueModificationActivitePrestataire`**). Colonnes communes : **`ancienne_valeur`** et **`nouvelle_valeur`** (TEXT, **libellés lisibles**). Enum **`HistoriqueModificationAction`** (**`CREATION`**, **`MODIFICATION`**, **`SUPPRESSION`**). Index notables : **`idx_hist_mod_ligne_jour`**, **`idx_hist_mod_activite`**, **`idx_hist_mod_cahier_inf`**, **`idx_hist_mod_chambre`**, **`idx_hist_mod_ap`**. **`HistoriqueModificationService`** / **`HistoriqueModificationServiceImpl`** : enregistrement et consultation par type. **Snapshots** : **`ActiviteServiceImpl`**, **`PlanningGrilleServiceImpl`**, **`CahierInfirmerieServiceImpl`**, **`ChambreServiceImpl`**, **`ActivitePrestataireServiceImpl`** (signatures techniques + libellés sans ids SQL). Endpoints : **`GET .../activites/{activiteId}/historique`**, **`GET .../planning-grilles/.../historique-cellules?jour=`**, **`GET .../cahier-infirmerie/{entreeId}/historique`**, **`GET .../chambres/{chambreId}/historique`**, **`GET .../activites-prestataires/{activitePrestataireId}/historique`** (tous **`ACCES_SEJOUR`** + appartenance). Extensions par domaine : voir puces dédiées ci-dessus et [etat-projet.md](./etat-projet.md). Contrats : [documentation-api-rest.md](./documentation-api-rest.md).
  - **Sanitaire — chargement groupé des dossiers enfants du séjour** : **`GET /api/v1/sejours/{sejourId}/dossiers-enfants`** dans **`SejourController`** ; **`EnfantServiceImpl`** : **`listerDossiersEnfantsDuSejour`** (contrôle **directeur / équipe** comme **`getEnfantsDuSejour`**), une ligne par enfant inscrit, **`groupes`** via tous les **`Groupe`** du séjour (fetch enfants), **`dossier`** mappé comme le GET unitaire ou **`null`**. Repos : **`GroupeRepository.findBySejourIdFetchingEnfants`**, **`SejourEnfantRepository.findBySejourIdWithEnfant`** avec **`ORDER BY enfant.nom, enfant.prenom`**. DTOs : **`EnfantDossierSanitaireLigneDto`**, **`GroupeResumeDto`**. **`SejourController`** injecte **`SejourService`** + **`EnfantService`**. Tests : **+4** **`EnfantServiceImplTest`**, **+1** **`SejourControllerTest`** ; agrégat Surefire **346** tests (**`mvn test`**, 2026-05-08).
  - **Lieux — usages métier (`UsageLieu`)** : Enum **`ACTIVITE`**, **`SURVEILLANCE`**, **`RASSEMBLEMENT`** ; au moins un obligatoire en création / édition via **`SaveLieuRequest.usages`** (**`@NotEmpty`**). Entité **`Lieu`** : **`@ElementCollection`** **`lieu_usage`** (**`lieu_id`**, **`usage_lieu`** STRING). **`LieuUsageRules`** (**`utils/`**) : **`usagesEffectifs`** — si collection vide ou absente en persistance → **`ACTIVITE`** seul (données historiques sans table remplie) ; **`acceptePourActivite`** ; **`acceptePourPlanningSurveillanceOuRassemblement`** ; **`usagesEnOrdreDeclaration`** pour sérialisation DTO stable. **`LieuServiceImpl`** : **`usagesDepuisRequete`** (filtrage `null`, ensemble non vide). **`ActiviteServiceImpl.resoudreLieuPourSejour`** : refuse un lieu sans usage **`ACTIVITE`** (**`IllegalArgumentException`**, message avec id lieu). **`PlanningGrilleServiceImpl`** : **`resoudreLieuPourPlanning`** pour lignes et cellules source **LIEU** — lieu doit avoir **`SURVEILLANCE`** ou **`RASSEMBLEMENT`** sinon même type d’erreur. **`LieuDto`** enrichi (**`Set<UsageLieu> usages`** + **`sejourId`**). **Migration BDD** recommandée : création table **`lieu_usage`** + **`INSERT`** **`ACTIVITE`** pour chaque **`lieu.id`** encore absent de la table (scripts fournis hors dépôt ou phpMyAdmin). **`mvn test`** : **346** tests (somme Surefire `Tests run:`, 2026-05-08 ; dont **`LieuServiceImplTest` 9**, **`ActiviteServiceImplTest` 25**, **`PlanningGrilleServiceImplTest` 9**).
  - **Animateurs — activités, menus, références alimentaires (alignement droits)** :
    - **Activités** `POST/PUT/DELETE` : `@PreAuthorize("hasAuthority('ACCES_SEJOUR')")` ; **POST** : `ActiviteServiceImpl.creerActivite(..., utilisateurTokenId)` appelle **`verifierAppartenanceAuSejour`** en tête (aucune création hors séjour). **PUT/DELETE** : après chargement de l’entité, **`verifierDroitModificationOuSuppressionActivite`** : **ADMIN** ; ou **`aDroitGestionCompleteSurSejour`** (directeur du séjour, membre équipe avec privilège **`GESTION_SEJOURS`**, ex. **ADJOINT**) ; sinon **appartenance** + présence du **`tokenId`** dans **`activite.membres`**. Message **`AccessDeniedException`** explicite si animateur non affecté. **`SejourVerificationService`** : **`aDroitGestionCompleteSurSejour`**, **`verifierDroitModificationOuSuppressionActivite`** ; **`verifierDroitGestionSejour`** refactorisée pour réutiliser la même logique « gestion complète ».
    - **Menus** — **`GET /api/v1/sejours/{sejourId}/menus`** (paramètres **`date`** ou **`dateDebut`+`dateFin`**) et **`GET .../menus/{menuId}`** : **`ACCES_SEJOUR`** ; services **`listerParJour`**, **`listerParPeriode`**, **`get`** prennent **`utilisateurTokenId`** et appellent **`verifierAppartenanceAuSejour`**. **POST/PUT/DELETE** menus : inchangés (**`GESTION_SEJOURS`**).
    - **Agrégation refs dossiers enfants** — **`GET .../references-alimentaires-agregees-enfants`** : **`ACCES_SEJOUR`** + **`agregerPourSejour(sejourId, utilisateurTokenId)`** avec appartenance.
    - **Référentiel global** **`/api/v1/references-alimentaires`** : **`GET`** (liste + **`/{id}`**) en **`ACCES_SEJOUR`** (animateurs avec JWT incluant ce privilège) ; **POST/PUT/DELETE** restent **`ROLE_ADMIN` ou `ROLE_DIRECTION`** (message 403 contextualisé existant pour échecs sur ce chemin hors lecture).
    - **Tests** : **`ActiviteServiceImplTest`** (stub admin lenient, scénarios refus / succès animateur assigné), **`ActiviteControllerTest`** (principal sur **POST**). **`mvn test`** OK.
  - **403 : messages d’accès refusé contextualisés (JSON `ErrorResponse`)** : Spring Security renvoie souvent un message technique peu utile pour le front. **`CustomAccessDeniedHandler`** résout un **`message`** lisible selon **`request.getRequestURI()`** (query string ignorée pour le matching) et la méthode HTTP : CRUD séjours réservés aux **admins plateforme** (`GET/POST/PUT/DELETE` sur `/api/v1/sejours` et `/api/v1/sejours/{id}`) ; référentiel **`/references-alimentaires`** global (hors préfixe **`/sejours/{id}/`**) ; périmètre **`/utilisateurs`** (dont **`/search`** pour l’ajout équipe) ; **`GET /api/v1/sejours/utilisateur/...`** ; **`PUT`** sur **`.../enfants/.../dossier`** (droit sanitaire / direction / adjoint) ; tout autre chemin contenant **`/sejours/`** → message « gestion réservée au directeur ou adjoint avec droits » ; défaut → message générique. Les messages **informatifs** laissés par Spring (hors variantes « access denied » génériques) sont **conservés**. **`GlobalExceptionHandler`** : handler **`AccessDeniedException`** pour les refus **`@PreAuthorize`** en couche contrôleur (non pris par le **`AccessDeniedHandler`** des filtres), en réutilisant **`CustomAccessDeniedHandler.resolveMessageForClient`**. Logs **`error` → `warn`** côté handler filtre. Fichiers : **`config/CustomAccessDeniedHandler.java`**, **`handlers/GlobalExceptionHandler.java`**. Protocole Memory Bank : [`.cursorrules`](.cursorrules) **§7**.
  - **Extension `GESTION_SEJOURS` aux actions “direction de séjour”** : Harmonisation des contrôleurs séjour : endpoints de gestion équipe (`SejourController`), groupes, activités, lieux, horaires, moments, types d’activité, plannings, menus et agrégation références enfants passent en `@PreAuthorize("hasAuthority('GESTION_SEJOURS')")` (ADJOINT inclus via `RoleSejour`). Endpoints globaux (`ADMIN`/référentiels globaux) conservés. Validation par `mvn test` OK.
  - **Droits enfants alignés sur `GESTION_SEJOURS` (ADJOINT effectif)** : Les opérations de gestion enfants (création, modification, suppression, import) utilisent désormais `@PreAuthorize("hasAuthority('GESTION_SEJOURS')")` au lieu de `hasRole('DIRECTION')`. Les autorités Spring incluent maintenant aussi les autorités `RoleSejour` via `Utilisateur.getAuthorities()` (fusion rôle global + équipe séjour). Chargement auth adapté via `findWithSejoursEquipeByEmail/findWithSejoursEquipeByTokenId`. Les méthodes de gestion `EnfantService` propagent `utilisateurTokenId` et `EnfantServiceImpl` applique `SejourVerificationService.verifierDroitGestionSejour` pour conserver un contrôle **par séjour** (pas de droit transversal entre séjours). Tests contrôleurs/services/auth réalignés, `mvn test` OK.
  - **Lecture des plannings ouverte aux membres d'équipe** : Les endpoints **`GET /api/v1/sejours/{sejourId}/planning-grilles`** et **`GET /api/v1/sejours/{sejourId}/planning-grilles/{grilleId}`** passent de **`hasRole('DIRECTION')`** à **`hasAuthority('ACCES_SEJOUR')`**, alignés sur le pattern déjà appliqué à **`Activite`**, **`Horaire`** et **`Lieu`**. Les **écritures complètes** (**`POST` / `PUT` / `DELETE`** sur grilles, lignes, **`PUT .../cellules`** en masse) : **`hasAuthority('GESTION_SEJOURS')`**. **Membre d'équipe** avec **`ACCES_SEJOUR`** : **`PATCH .../cellules/{jour}/ma-presence`** pour s'inscrire / se désinscrire seulement lorsque **`sourceContenuCellules` = `MEMBRE_EQUIPE`** (voir puce **Planning — PATCH** ci-dessus). **Service** : `PlanningGrilleService.listerGrilles(int sejourId, String utilisateurTokenId)` et `getGrille(int sejourId, int grilleId, String utilisateurTokenId)` ; l'impl appelle **`SejourVerificationService.verifierAppartenanceAuSejour`** avant la lecture (court-circuit ADMIN, sinon vérifie directeur **OU** membre de l'équipe ; sinon **`AccessDeniedException`** → 403). **Contrôleur** : injection de `Authentication authentication`, principal cast en `Utilisateur`, transmission du `tokenId`. **Tests** : `PlanningGrilleControllerTest` (`.principal(authentication)` sur les `GET`), `PlanningGrilleServiceImplTest` (appartenance **`listerGrilles`**, refus hors séjour, **ligne `LIEU`** lieu activité seule refusé / rassemblement accepté, **ma-présence membre équipe**).
  - **Réalignement complet de la suite de tests sur les nouvelles signatures `tokenId`** : Après l'ajout du paramètre **`String utilisateurTokenId`** sur de nombreuses méthodes de services (cf. point ci-dessous sur **`ACCES_SEJOUR`** et le contrôle d'appartenance) et le passage du constructeur **`SejourVerificationService(SejourRepository, UtilisateurRepository)`**, mise à jour systématique de toute la suite de tests. **Pattern unifié pour les tests de service impl** (`ActiviteServiceImplTest`, `HoraireServiceImplTest`, `LieuServiceImplTest`, `GroupeServiceImplTest`, `PlanningGrilleServiceImplTest`, `SejourServiceImplTest`) : injection d'un mock **`UtilisateurRepository`** quand absent, fixture **`appelantAdmin`** (`Role.ADMIN`, `tokenId = "appelant-token"`) stubbé via **`utilisateurRepository.findByTokenId("appelant-token")`** pour bypasser **`verifierAppartenanceAuSejour`** (le branchement ADMIN court-circuite la vérification de séjour) ; pour les scénarios « séjour absent », utilisation d'un user **non-ADMIN** (`Role.BASIC_USER`) afin que la vérification atteigne **`verifierSejourExiste`** et lève **`ResourceNotFoundException`**. **Pattern unifié pour les tests de contrôleur en `MockMvcBuilders.standaloneSetup`** (`ActiviteControllerTest`, `HoraireControllerTest`, `LieuControllerTest`, `EnfantControllerTest`, `GroupeControllerTest`, `SejourControllerTest`) : création d'un `Utilisateur` factice (tokenId `"user-token-123"`) encapsulé dans une **`UsernamePasswordAuthenticationToken`**, injecté à chaque requête concernée via **`mockMvc.perform(get(...).principal(authentication))`** — sans quoi `authentication.getPrincipal()` provoque un NPE car `standaloneSetup` ne câble aucun contexte de sécurité. **Stubs et `verify(...)` mis à jour** pour la nouvelle signature à 2/3 arguments. **`mvn test`** : **346** tests (Surefire, 2026-05-08). **Mémo** : `MockMvcRequestBuilders.principal(Principal)` reste la voie la plus simple pour ce besoin, sans dépendance à `spring-security-test` ni `SecurityContextHolder`.
  - **API Séjours par utilisateur avec gestion des droits** : Refonte complète de l'endpoint de récupération des séjours par utilisateur. **Ancien endpoint déprécié** : `GET /api/v1/sejours/directeur/{directeurTokenId}` (permissions `DIRECTION` uniquement, retournait uniquement les séjours où l'utilisateur était directeur). **Nouvel endpoint** : `GET /api/v1/sejours/utilisateur/{utilisateurTokenId}` (permissions `ADMIN`, `DIRECTION`, `BASIC_USER`). **Logique par rôle** : ADMIN récupère **tous** les séjours du système via `sejourRepository.findAll()`, autres utilisateurs récupèrent uniquement les séjours où ils participent (directeur **OU** membre d'équipe) via nouvelle requête JPQL `SejourRepository.findSejoursByUtilisateur()`. **Requête JPQL** : `SELECT DISTINCT s FROM Sejour s LEFT JOIN s.equipeRoles se WHERE s.directeur = :utilisateur OR se.utilisateur = :utilisateur` (évite les doublons si un utilisateur est à la fois directeur et dans l'équipe). **Interface/Service** : `SejourService.getSejoursByUtilisateur(String utilisateurTokenId)` remplace `getSejoursByDirecteur`. **Tests** : `SejourControllerTest` et `SejourServiceImplTest` mis à jour (renommage des méthodes de test, ajout d'un test spécifique pour le comportement ADMIN qui vérifie l'appel à `findAll()` et non à `findSejoursByUtilisateur()`). **Documentation frontend** : fichier [API_CHANGEMENT_SEJOURS_PAR_UTILISATEUR.md](../API_CHANGEMENT_SEJOURS_PAR_UTILISATEUR.md) créé avec exemples d'utilisation, codes HTTP, guide de migration JavaScript avant/après, et points d'attention (permissions élargies, filtrage automatique backend, ADMIN voit tout).
  - **Renommage privilège `ACCES_SEJOUR` et amélioration qualité du code** : Renommage du privilège `GESTION_PLANNINGS` → `ACCES_SEJOUR` (nom plus représentatif, car ce privilège donne l'accès de base aux données d'un séjour : consultation enfants, groupes, etc., pas seulement plannings). Suppression des imports statiques `import static Privilege.*` dans `Role` et `RoleSejour` : utilisation explicite de `Privilege.GESTION_UTILISATEURS`, `Privilege.ACCES_SEJOUR`, etc. pour plus de clarté, de sécurité et pour éviter les conflits de noms. Finalisation des permissions d'accès : **DIRECTION** (CRUD complet sur équipe/enfants/groupes), **AS** avec `GESTION_SANITAIRE` (consultation + modification dossiers enfants uniquement), **autres membres** avec `ACCES_SEJOUR` seul (consultation uniquement). Vérification systématique d'appartenance au séjour (directeur ou membre d'équipe) dans `EnfantServiceImpl.getEnfantsDuSejour()`, `GroupeServiceImpl.getGroupesDuSejour()` et `getGroupeById()`. Tests mis à jour avec `utilisateurTokenId` et mocks ajustés.
  - **Privilèges `RoleSejour` indépendants du `Role` global** : Les `RoleSejour` ont leurs propres privilèges, **indépendants** du `Role` global de l'utilisateur. Chaque `RoleSejour` définit directement ses privilèges : ANIM/SB/AUTRE → `ACCES_SEJOUR` uniquement, AS → `ACCES_SEJOUR` + `GESTION_SANITAIRE`, ADJOINT → `GESTION_SEJOURS` + `GESTION_SANITAIRE` + `ACCES_SEJOUR`. Pas de rôle `SANITAIRE` dans l'enum `Role` (seulement `ADMIN`, `DIRECTION`, `BASIC_USER`). Les privilèges du `RoleSejour` sont utilisés uniquement dans le contexte du séjour et **ne modifient pas** le `Role` global de l'utilisateur dans la table `utilisateur`. Le `Role` global et le `RoleSejour` sont deux systèmes séparés : le `Role` contrôle l'accès aux fonctionnalités globales de l'application, le `RoleSejour` contrôle les droits dans le contexte d'un séjour spécifique. Méthodes `RoleSejour.getPrivileges()` et `getAuthorities()` pour récupérer les privilèges et autorités Spring Security.
  - **Suppression de groupe - Vérification des activités associées** : Avant de supprimer un groupe, le système vérifie maintenant si des activités y sont encore associées. Si c'est le cas, une **`IllegalStateException`** est levée avec un message explicite indiquant le nom du groupe et le nombre d'activités concernées (ex : *« Impossible de supprimer le groupe 'Les Ados' : 3 activité(s) sont encore associée(s) à ce groupe. Veuillez d'abord modifier ou supprimer ces activités. »*). **`GlobalExceptionHandler`** : nouveau handler pour **`IllegalStateException`** retournant **HTTP 409 (Conflict)**. **`ActiviteRepository`** : nouvelle méthode **`countByGroupeId(groupeId)`** (JPQL sur jointure `groupes`). **`GroupeServiceImpl`** : injection de **`ActiviteRepository`**, vérification dans **`supprimerGroupe`** avant `delete()`. Tests : **`GroupeServiceImplTest`** ajusté (mock `activiteRepository` dans setup, test du cas nominal avec 0 activités, nouveau test vérifiant l'exception avec 3 activités). **`mvn test`** OK après évolutions (total agrégé Surefire **~346** en 2026-05-08).
  - **Menus & alimentation (séjour) — commit `feat: add menus CRUD, food references, and aggregated child tags`** : Entité **`MenuRepas`** liée au **`Sejour`** (`dateRepas`, **`TypeRepas`**, champs structurés entrée/plat/fromage/dessert ou texte petit-déj/goûter, M2M **`ReferenceAlimentaire`** pour allergènes et régimes-préférences ; contrainte d’unicité **`(sejour_id, date_repas, type_repas)`**). API **`/api/v1/sejours/{sejourId}/menus`** : liste avec query **`date`** *ou* **`dateDebut` + `dateFin`**, CRUD (à l’origine tout **`DIRECTION`** ; **état actuel** : **`GET`** **`ACCES_SEJOUR`** + appartenance, écritures **`GESTION_SEJOURS`** — voir puce **Animateurs** ci-dessus). Référentiel global **`ReferenceAlimentaire`** + enum **`TypeReferenceAlimentaire`** ; CRUD **`/api/v1/references-alimentaires`** (filtre optionnel `type`), **`ADMIN` ou `DIRECTION`** ; **`ReferenceAlimentaireInitializer`** (données par défaut au démarrage). **`DossierEnfant`** : M2M mêmes références (`dossier_enfant_allergene`, `dossier_enfant_regime_preference`) ; **`UpdateDossierEnfantRequest`** : **`allergeneIds`**, **`regimePreferenceIds`** ; **`DossierEnfantDto`** : listes **`ReferenceAlimentaireDto`**. **`GET /api/v1/sejours/{sejourId}/references-alimentaires-agregees-enfants`** : union des tags présents sur les dossiers des enfants inscrits au séjour (sans doublon), pour aider la saisie des menus. Services : **`MenuRepasServiceImpl`**, **`ReferenceAlimentaireServiceImpl`**, **`ReferencesAlimentairesAgregeesEnfantsServiceImpl`** ; repos **`MenuRepasRepository`**, **`ReferenceAlimentaireRepository`** ; **`DossierEnfantRepository`** : **`findByEnfantIdFetchingReferences`**, **`findByEnfantIdInFetchingReferences`**. **`EnfantServiceImpl`** : mapping dossier ↔ ids / DTOs références ; **`EnfantServiceImplTest`** ajusté.
  - **Contrat API `Utilisateur` (JSON)** : toute **référence utilisateur** front ↔ back = **`tokenId`**, jamais l’`id` SQL — archivé en **`.cursorrules`** (*Utilisateurs : identifiant côté API*) et dans [decisions-architecturales.md](./decisions-architecturales.md).
  - **Plannings direction (grille générique)** : **`PlanningGrille`** / **`PlanningLigne`** / **`PlanningCellule`**. Libellé de **ligne** = une seule référence selon **`sourceLibelleLignes`**. **Cellules** : **`ManyToMany`** animateurs (`membreTokenIds`) et horaires / moments / groupes / lieux (tables `planning_cellule_*`) ; JSON cellules en **listes** uniquement. **Références lieu** (ligne ou cellule, source **LIEU**) : lieux avec **`SURVEILLANCE`** et/ou **`RASSEMBLEMENT`** uniquement (`LieuUsageRules`). API et règles : [documentation-api-rest.md](./documentation-api-rest.md) (section *Plannings — grilles*). Modèle / migration BDD : [etat-projet.md](./etat-projet.md). Guide front : [frontend-planning-cellules-multiples.md](../frontend-planning-cellules-multiples.md).
  - **Entité `Horaire` & CRUD** : Libellés du type **`6h00`**, **`7h15`**, **`18h30`** (pattern **`Horaire.LIBELLE_HORAIRE_PATTERN`**). Entité **`Horaire`** : **`libelle`**, **`ManyToOne`** obligatoire vers **`Sejour`** ; contrainte **`uk_horaire_sejour_libelle`**. **`HoraireRepository`** : **`findBySejourIdOrderByIdAsc`**, **`findByIdAndSejourId`**, **`existsBy*`** (unicité insensible à la casse côté service → **409**). API sous **`/api/v1/sejours/{sejourId}/horaires`** : **`GET`** **`ACCES_SEJOUR`** + appartenance ; **`POST` / `PUT` / `DELETE`** **`GESTION_SEJOURS`**. **`SaveHoraireRequest`**, **`HoraireDto`**. Tests : **`HoraireControllerTest`** (**7**), **`HoraireServiceImplTest`** (**7**). Détail : [documentation-api-rest.md](./documentation-api-rest.md).
  - **Activité : unicité animateur par créneau (séjour + jour + moment, hiérarchie incluse)** : Un même membre d’équipe ne peut **pas** être planifié sur **deux activités** pour le **même séjour**, la **même `LocalDate`** et des moments qui **se chevauchent** (même moment **ou** lien ancêtre/descendant — voir puce « conflit animateur étendu à la hiérarchie » en tête). Vérification dans **`ActiviteServiceImpl.verifierMembresDisponiblesPourCreneau`** (calcul de l’ensemble via **`momentsEnConflit`**) ; requête **`ActiviteRepository.findMomentsEnConflitPourMembre(sejourId, date, momentIds, utilisateurId, excludeActiviteId)`** (JPQL sur la jointure **`membres`**, `excludeActiviteId` = id de l’activité exclue en **PUT**, `null` en **POST**). En conflit : exception **`ConflitPlanningAnimateurException`** (pas **`IllegalArgumentException`**). **Code API stable** (ASCII) : **`ConflitPlanningAnimateurException.CODE`** = **`ANIMATEUR_DEJA_AFFECTE_CRENEAU`**. **`GlobalExceptionHandler`** : handler dédié → HTTP **400**, corps JSON **`code`**, **`message`**, **`error`** (même texte que `message` pour rétrocompatibilité). **Message utilisateur** (prénom, sinon nom si prénom vide) cite le **moment réellement occupé** et le chevauchement si le nom diffère — **`DateFormatHelper.formatDdMmYyyy`**. *Tests* : scénarios conflit création / modification + exclusion de l’activité courante en **`ActiviteServiceImplTest`**.
  - **Audit codebase (mémo)** : Projet **sans Lombok** ; `ErrorResponse` = classe + **`ErrorResponseBuilder`** interne manuel ; injection par **constructeurs explicites** partout. Comptage tests (agrégat Surefire 2026-05-08) : **`SejourServiceImplTest` ~36**, **`UtilisateurServiceImplTest` ~32**, **`EnfantServiceImplTest` ~27**, **`TypeActiviteServiceImplTest` ~6**. Contrôleurs testés : **`HoraireControllerTest`** (**7**) ; pas de **`MomentControllerTest`** ni **`TypeActiviteControllerTest`** (autres contrôleurs : voir puce « Mémo projet (audit) »).
  - **Activité : type d’activité obligatoire** — `Activite.typeActivite` en **`@ManyToOne(optional = false)`**, **`type_activite_id` NOT NULL** ; **`CreateActiviteRequest` / `UpdateActiviteRequest`** : **`typeActiviteId`** **`@NotNull`** ; **`ActiviteDto.typeActivite`** attendu pour toute activité en base cohérente ; **migration** : backfill des lignes sans type avant contrainte (voir puce « Migration BDD » ci-dessous + § endpoints activités).
  - **Types d’activité (`TypeActivite`) — par séjour** : Entité JPA **`TypeActivite`** rattachée à un **`Sejour`** (`ManyToOne` obligatoire, `sejour_id`). Contrainte **`uk_type_activite_sejour_libelle`** : **unicité du libellé par séjour** (insensible à la casse côté service) ; le même libellé peut exister sur **deux séjours différents**. Champ **`predefini`** : `true` pour les six types livrés par défaut (**Sport, Manuel, Expression, Jeux, Cuisine, Musique**, liste **`TypeActiviteLibellesParDefaut`**) — **non modifiables / non supprimables** via l’API ; `false` pour les types créés par **`POST .../types-activite`**. Méthode **`TypeActiviteService.assurerTypesParDefautPourSejour(sejourId)`** : idempotent (crée les manquants, corrige **`predefini`** sur les libellés connus déjà présents). Appelée depuis **`SejourServiceImpl.creerSejour`** après sauvegarde du séjour, et depuis **`TypeActiviteInitializer`** au démarrage pour **chaque** séjour en base (`SejourRepository.findAll()`). **`TypeActiviteRepository`** : **`findBySejourIdOrderByLibelleAsc`**, **`findByIdAndSejourId`**, **`findBySejourIdAndLibelleIgnoreCase`**, **`existsBySejourIdAndLibelleIgnoreCase`**, **`existsBySejourIdAndLibelleIgnoreCaseAndIdNot`**. **`ActiviteServiceImpl`** résout **`typeActiviteId`** avec **`findByIdAndSejourId`** (type doit appartenir au **même** séjour que l’activité) ; **`typeActiviteId`** est **obligatoire** sur création / mise à jour d’activité (**`@NotNull`** + **`type_activite_id` NOT NULL** en entité). API **`/api/v1/sejours/{sejourId}/types-activite`** (comme lieux / moments). **`TypeActiviteDto`** : `id`, `libelle`, `predefini`, **`sejourId`**. **`SejourServiceImpl`** : **8** champs (incl. **`TypeActiviteService`**, **`ActiviteRepository`**). **`TypeActiviteServiceImpl`** : **`TypeActiviteRepository`**, **`ActiviteRepository`**, **`SejourRepository`**. *Tests* : **`TypeActiviteServiceImplTest`** (mockito, scénarios création / verrou prédéfini / **`assurerTypesParDefaut`**) ; **`SejourServiceImplTest`** vérifie **`assurerTypesParDefautPourSejour`** après **`creerSejour`** ; pas de **`TypeActiviteControllerTest`** pour l’instant. *Migration BDD* : passage d’un référentiel global à **`sejour_id` NOT NULL** peut exiger nettoyage d’anciennes lignes / index ; voir déploiement. *Frontend* : URLs avec **`sejourId`**, **`TypeActiviteDto.sejourId`**, services CRUD sous le séjour.
  - **Entité `Moment` & CRUD** : Créneaux du type « Matin », « Après-midi », etc., **un par séjour** (`ManyToOne` `Sejour`), **unicité du nom par séjour** (contrainte `uk_moment_sejour_nom`, doublon ignorant la casse côté service → `ResourceAlreadyExistsException` 409). Champ **`ordre`** (`Integer`, nullable en BDD pour lignes historiques) : **tri d’affichage chronologique** `ORDER BY COALESCE(ordre, id), id` via **`MomentRepository.findBySejourIdOrderChronologique`**. **Création** : `ordre` = max sur le séjour de `ordre` s’il est renseigné sinon `id`, puis **+1** (premier moment → **0**). **Réordonnancement** : **`PUT .../moments/reorder`**, body **`ReorderMomentsRequest`** (`momentIds` : liste **complète** des ids du séjour dans le **nouvel ordre** ; positions persistées **0 … n−1**) ; incohérences / doublons → **`IllegalArgumentException`** (**400**). DTO **`MomentDto`** : `id`, `nom`, `sejourId`, **`ordre`** (fallback affichage : `id` si `ordre` null). `Sejour.moments` en `OneToMany` cascade + orphanRemoval. API **`/api/v1/sejours/{sejourId}/moments`** : **`GET`** **`ACCES_SEJOUR`** + appartenance ; **`POST` / `PUT` / `DELETE`** et **`PUT .../reorder`** **`GESTION_SEJOURS`** (voir **`MomentController`**). Payloads : **`SaveMomentRequest`** (`nom` seul — pas d’`ordre` en création/édition nom), **`ReorderMomentsRequest`**. **`MomentRepository`** : `countBySejourId`, **`findBySejourIdOrderChronologique`**, `findByIdAndSejourId`, `existsBySejourIdAndNomIgnoreCase*`. Suppression refusée si **`ActiviteRepository.existsByMomentId`** **ou** **`ActivitePrestataireRepository.existsByMoments_Id`** → **400** (`IllegalArgumentException`). *Migration* : colonne `ordre` ajoutée par **`ddl-auto: update`** ; les lignes existantes peuvent avoir `ordre` null jusqu’à un appel **`PUT .../moments/reorder`** ou rester triées comme avant via `COALESCE(ordre, id)`.
  - **Activité ↔ Moment (obligatoire)** : `Activite.moment` **`@ManyToOne` obligatoire** (`moment_id`). **`CreateActiviteRequest` / `UpdateActiviteRequest`** : **`momentId`** (`Integer`, validé dans le service : si `countBySejourId == 0` → **400** avec message demandant à la **direction** de créer des moments ; si moments existent et `momentId == null` → **400** moment obligatoire ; sinon résolution via `MomentRepository.findByIdAndSejourId`). **`ActiviteDto`** inclut **`MomentDto moment`** (non null en sortie).
  - **Activité ↔ type d’activité (obligatoire)** : `Activite.typeActivite` **`@ManyToOne(optional = false)`**, jointure **`type_activite_id` NOT NULL**. **`CreateActiviteRequest` / `UpdateActiviteRequest`** : **`typeActiviteId`** avec **`@NotNull`** (message « Le type d’activité est obligatoire ») ; résolution via **`TypeActiviteRepository.findByIdAndSejourId`** ; si `null` malgré tout côté service → **`IllegalArgumentException`** (**400**) ; type inconnu ou pas du séjour → **404**. DTO : **`ActiviteDto.typeActivite`** (`TypeActiviteDto`) **toujours renseigné** pour une activité cohérente ; mapping **`typeActiviteVersDto`** via **`Objects.requireNonNull`**.
  - **Lieu : partage — jour + moment** : Les comptages d’occupation pour le partage utilisent **même séjour, même lieu, même `LocalDate`, même `moment`** : **`ActiviteRepository.countBySejour_IdAndLieu_IdAndDateAndMoment_Id`** et **`...AndMoment_IdAndIdNot`** (exclusion de l’activité en cours de mise à jour). Les messages (conflit, limite, **`avertissementLieu`**) mentionnent le **nom du moment**. *Ancienne logique « même jour seulement » remplacée.*
  - **Migration BDD** : Ajout de **`moment_id` NOT NULL** sur `activite` : avec données existantes, risque d’échec `ddl-auto` ; prévoir création de moments + **backfill** des activités ou migration SQL avant contrainte stricte. Idem pour **`type_activite_id` NOT NULL** : toute activité encore sans type doit être **mise à jour** (type valide du séjour) ou **supprimée** avant d’appliquer la contrainte, sinon échec `ddl-auto` / migration. **Usages des lieux** : table **`lieu_usage`** (plusieurs lignes par **`lieu_id`**) ; anciens lieux sans ligne → assimilés **`ACTIVITE`** côté appli jusqu’à backfill **`INSERT`** **`ACTIVITE`** pour chaque lieu concerné en MySQL.
  - **Lieu : partage entre activités / animateurs** : Champs **`partageableEntreAnimateurs`** (`boolean`, défaut `false`) et **`nombreMaxActivitesSimultanees`** (`Integer`, `null` si non partageable). Le **plafond** s’applique au nombre d’activités sur **ce lieu, ce jour calendaire et ce moment** (voir comptages activités). Si partage activé : `nombreMaxActivitesSimultanees` **obligatoire** et **≥ 2** ; sinon **`IllegalArgumentException`** (400). Si partage désactivé : `nombreMaxActivitesSimultanees` doit rester **`null`**. Règles dans `LieuServiceImpl.validerParametresPartage`. `SaveLieuRequest` / `LieuDto` exposent ces champs.
  - **Activité : occupation d’un lieu (jour + moment)** : À la **création / mise à jour**, si `lieuId` est renseigné, `ActiviteServiceImpl` compte les **autres** activités avec **même lieu**, **même date** et **même moment** (méthodes `countBySejour_IdAndLieu_IdAndDateAndMoment_Id` / `...AndIdNot`). Comportement partage / limite / **`avertissementLieu`** inchangé par rapport à la doc historique, mais le **créneau (`Moment`)** est pris en compte. **`IllegalStateException`** si lieu partageable sans max.
  - **Activité ↔ Lieu / Moment / type (rappel)** : `Activite` **`@ManyToOne` optionnel** vers `Lieu` — le lieu doit **inclure l’usage `ACTIVITE`** (`LieuUsageRules`), sinon **400**. **`@ManyToOne` obligatoire** vers **`Moment`** et vers **`TypeActivite`**. Requêtes : **`lieuId`** optionnel, **`momentId`** requis dès qu’il existe au moins un moment pour le séjour, **`typeActiviteId`** toujours requis. `ActiviteDto` : **`moment`**, **`typeActivite`** (non null pour données cohérentes), **`lieu`** ( **`LieuDto.usages`** ), **`avertissementLieu`**. **Frontend** : à aligner (`api.d.ts`) — activ avec **`moment` / `momentId`**, **`typeActivite` / `typeActiviteId`**, **`lieu.usages` / SaveLieuRequest.usages** en création / mise à jour.
  - **Lieu & unicité du nom** : Entité `Lieu` en **POJO JPA** (constructeur vide, getters/setters). `LieuServiceImpl` et `LieuController` : **constructeurs explicites** (alignés avec le reste du projet sans Lombok). Création : `new Lieu()` + setters. Unicité du **nom par séjour** (insensible à la casse) via `LieuRepository` : `existsBySejourIdAndNomIgnoreCase`, `existsBySejourIdAndNomIgnoreCaseAndIdNot` ; doublon → `ResourceAlreadyExistsException` (409). Nom normalisé (`trim` ; chaîne vide si `null`). Tests : `LieuServiceImplTest` (**9** : incl. **usages vides** → 400, validation partage sans max, partage avec max 2 + usages multiples, + scénarios historiques doublon / trim / modifier / lister).
  - **Entité Lieu & API** : Lieux rattachés à un **seul** séjour : `nom`, `EmplacementLieu`, **`nombreMax`** optionnel, **`partageableEntreAnimateurs`**, **`nombreMaxActivitesSimultanees`**, **`usages`** (**`Set<UsageLieu>`** persisté en **`lieu_usage`** — **`ACTIVITE`**, **`SURVEILLANCE`**, **`RASSEMBLEMENT`**, au moins un via API). `ManyToOne` vers `Sejour` ; `Sejour.lieux` `OneToMany` cascade + orphanRemoval. `LieuRepository` : `findBySejour`, `findBySejourId`, **`findByIdAndSejourId`**, `existsBy*`. **`LieuController`** : **`GET`** **`ACCES_SEJOUR`** + appartenance ; **`POST` / `PUT` / `DELETE`** **`GESTION_SEJOURS`**. **`SaveLieuRequest`** / **`LieuDto`** : champs historiques + **`usages`** (**`@NotEmpty`**). **`LieuUsageRules`** pour règles exposées DTO et garde-fous **activités** / **planning**. Tests : **`LieuControllerTest`** (**7**), **`LieuServiceImplTest`** (**9**).
  - **Mémo projet (audit)** : `EnfantControllerTest` **19**, `EnfantServiceImplTest` **27**, `ExcelHelperTest` *(rapport Surefire peut afficher 0 exécutions selon config — vérifier localement)*, `LieuControllerTest` **7**, `LieuServiceImplTest` **9**, `HoraireControllerTest` **7**, `HoraireServiceImplTest` **7**, **`PlanningGrilleControllerTest` 7**, **`PlanningGrilleServiceImplTest` 12**, `ActiviteServiceImplTest` **25** (incl. conflit animateur créneau, **lieu sans usage ACTIVITE**), **`ActivitePrestataireServiceImplTest` 6**, **`TypeActiviteServiceImplTest` ~6**, **`SejourServiceImplTest` ~36**, **`UtilisateurServiceImplTest` ~32**, **`SejourControllerTest` ~28** (autres : `GroupeControllerTest`, `AuthenticationControllerTest`, `GlobalExceptionHandlerTest`, `JwtAuthenticationFilterTest`, `TokenControllerHandlerTest`, etc.). `ActiviteServiceImpl` : **8** dépendances (repos + services comme **`MomentRepository`**, **`TypeActiviteRepository`**), usages **`LieuUsageRules`** en appels statiques pour les lieux ; `@SuppressWarnings("null")` classe. **`ActivitePrestataireServiceImpl`** : **5** deps (**`ActivitePrestataireRepository`**, **`SejourVerificationService`**, **`MomentRepository`**, **`GroupeRepository`**, **`UtilisateurRepository`**). **`TypeActiviteServiceImpl`** : **`TypeActiviteRepository`**, **`ActiviteRepository`**, **`SejourRepository`**. **`SejourServiceImpl`** : incl. **`TypeActiviteService`** (bootstrap à la création de séjour). Pas de **`MomentControllerTest`** / **`TypeActiviteControllerTest`** / **`ActivitePrestataireControllerTest`** pour l’instant. Packaging WAR en tête de fiche.
  - **Entité Activité & API** (`Activite`) : `LocalDate` date, nom, description, `Sejour`, **moment obligatoire**, lieu optionnel (**si renseigné : lieu avec usage `ACTIVITE`**), **`typeActivite` obligatoire** (`TypeActivite` **du même séjour**, `type_activite_id` NOT NULL), `@ManyToMany` animateurs (`activite_membre_equipe`), groupes (`activite_groupe`). **`lieuId`**, **`momentId`**, **`typeActiviteId`** (**obligatoire**, id d’un **`TypeActivite`** du séjour), `membreTokenIds`, **`groupeIds`**. Date dans `dateDebut`–`dateFin` du séjour ; directeur du séjour autorisé hors `SejourEquipe`. `ActiviteRepository` : **`countBySejour_IdAndLieu_IdAndDateAndMoment_Id`**, **`countBySejour_IdAndLieu_IdAndDateAndMoment_IdAndIdNot`**, **`findMomentsEnConflitPourMembre`** (conflit animateur, hiérarchie incluse), **`existsByMomentId`**, **`countByTypeActivite_Id`**. CRUD `api/v1/sejours/{sejourId}/activites`. Tests : `ActiviteServiceImplTest` (**25**), `ActiviteControllerTest` (2).
  - **supprimerMembreEquipe - Retrait des référents** : Lorsqu'un membre est retiré de l'équipe d'un séjour (`DELETE /sejours/{id}/equipe/{membreTokenId}`), il est désormais automatiquement retiré de tous les groupes du séjour où il était référent. `SejourServiceImpl.supprimerMembreEquipe()` utilise `GroupeRepository.findBySejourId()` puis retire le membre de chaque groupe via `groupe.getReferents().removeIf()`. La suppression d'un utilisateur (`supprimerUtilisateur`) gère déjà la table `groupe_referent` via JPA (cascade implicite sur FK).
  - **ExcelImportSpec - Logique groupes (ET/OU)** : La détection des colonnes Excel utilise désormais une logique par groupes : chaque colonne a des groupes de mots-clés ; l'en-tête doit contenir **tous** les groupes (ET entre groupes), avec au moins un mot par groupe (OU dans un groupe). Ex : `emailParent1` = (email OU mail) ET parent ET 1. Structure `ColumnDef` avec `groupesMotsCles` (String[][]). `ExcelHelper.containsAllGroups()` pour la vérification. Colonnes parent 1/2 (email, téléphone) et traitements (matin, midi, soir, si besoin) utilisent cette logique pour éviter les ambiguïtés. `getColumnMappings()` retourne `Map<String, String[][]>`.
  - **Correction ExcelHelper DataFormatter (locale)** : Le `DataFormatter` d'Apache POI utilise par défaut la locale système. Sur une machine française, le séparateur décimal est la virgule (3,14) au lieu du point (3.14), ce qui faisait échouer le test `shouldExtractNumericDecimal`. Correction : `new DataFormatter(Locale.US)` pour garantir un format décimal cohérent (point) indépendamment de la machine.
  - **ExcelImportSpec et notice d'import Excel** : Classe `ExcelImportSpec` (`excel/`) centralisant la spécification des colonnes pour l'import Excel des enfants. Source unique de vérité : toute modification des colonnes ou de leurs noms se fait dans `ExcelImportSpec` et s'applique automatiquement à l'import et à l'API. Structure `ColumnDef` avec `groupesMotsCles` (String[][]) : chaque groupe = alternatives (OU), tous les groupes doivent matcher (ET). Ex : `emailParent1` = [["email","mail"], ["parent"], ["1"]]. DTOs `ExcelImportColumnSpec` (champ, libelle, motsCles, obligatoire) et `ExcelImportSpecResponse` (colonnesObligatoires, colonnesOptionnelles, formatsAcceptes). Endpoint `GET /api/v1/sejours/{sejourId}/enfants/import/spec`. `ExcelHelper.detectColumns()` accepte `Map<String, String[][]>` et utilise `containsAllGroups()`. 
  - **Endpoint PUT dossier enfant** : Nouvel endpoint `PUT /api/v1/sejours/{id}/enfants/{enfantId}/dossier` pour modifier le dossier d'un enfant. `UpdateDossierEnfantRequest` (record) avec tous les champs optionnels du dossier. Vérification d'accès : directeur du séjour ou membre de l'équipe (sinon `AccessDeniedException` 403). `getDossierEnfant` et `modifierDossierEnfant` dans `EnfantService`/`EnfantServiceImpl`.
  - **Import Excel DossierEnfant (correction)** : Lors de l'import Excel, si un enfant existant possède déjà un `DossierEnfant`, on met à jour le dossier existant au lieu d'en créer un nouveau. Cela évite la violation de contrainte unique sur `enfant_id` (erreur SQL 1062 "Duplicate entry for key UKok865k0nbygu5b0xendcnl997"). Méthode `populateDossierFromExcelRow()` extraite pour centraliser le remplissage des champs optionnels du dossier depuis les colonnes Excel. Utilisation de `dossierEnfantRepository.findByEnfantId()` pour récupérer le dossier existant avant mise à jour.
  - **Entité Groupe** : Nouvelle entité `Groupe` liée à `Sejour` (ManyToOne). Trois types : `THEMATIQUE` (enfants manuels), `AGE` (tranche ageMin-ageMax, ajout auto), `NIVEAU_SCOLAIRE` (tranche niveauScolaireMin-Max, ajout auto). Relations `@ManyToMany` avec `Enfant` (table `groupe_enfant`) et `Utilisateur` (table `groupe_referent`) — migration depuis entités de jointure explicites (GroupeEnfant, GroupeReferent supprimées). `GroupeService`/`GroupeServiceImpl`, `GroupeController` (CRUD + ajout/retrait enfants et référents). `GroupeDto`, `CreateGroupeRequest`, `AjouterReferentRequest`. Conversion `Date` → `LocalDate` via `toLocalDate()` (évite `UnsupportedOperationException` sur `java.sql.Date.toInstant()`). Documentation frontend : `docs/frontend-creation-groupes.md`.
  - **Entité DossierEnfant** : Nouvelle entité `DossierEnfant` en relation OneToOne avec `Enfant` pour stocker les informations de dossier (contacts parents : emailParent1/2, telephoneParent1/2 ; infos médicales, PAI, alimentaires ; traitements : matin, midi, soir, si besoin ; autres infos, à prendre en sortie). `DossierEnfantRepository` avec `findByEnfantId()`. Création automatique du dossier lors de `creerEtAjouterEnfantAuSejour()` (dossier vide par défaut) et lors de l'import Excel (colonnes optionnelles). Lors de l'import Excel, si l'enfant existe déjà et qu'il a un dossier, on met à jour le dossier existant au lieu d'en créer un nouveau. `DossierEnfantDto` (record) pour les réponses. `ExcelHelper.normalizePhone()` pour normaliser les numéros de téléphone. `EnfantServiceImpl` injecte `DossierEnfantRepository`.
  - **Restructuration handlers/exceptions** : `GlobalExceptionHandler` déplacé de `exceptions/` vers `handlers/` pour une séparation cohérente : `exceptions/` contient les classes d'exception métier (`ResourceNotFoundException`, `TokenException`, etc.), `handlers/` contient les ExceptionHandler (`GlobalExceptionHandler`, `TokenControllerHandler`) et `ErrorResponse`. Imports mis à jour dans tous les tests.
  - **Utilisation de l'enum Genre dans Utilisateur** : L'entité `Utilisateur` utilise maintenant `Genre` (enum) au lieu de `String` pour le champ genre, aligné avec `Enfant`. Méthode statique `Genre.parseGenre(String)` ajoutée pour parser les valeurs API ("Masculin", "Féminin", "Garçon", "Fille", "Homme", "Femme", "M", "F"). `ProfilDto` utilise aussi `Genre`. `RegisterRequest` et `UpdateUserRequest` gardent `String` pour l'API (parsing dans les services). `EnfantServiceImpl` utilise désormais `Genre.parseGenre()` au lieu de sa méthode privée.
  - **Correction ExcelHelperTest** : Suppression de `@SuppressWarnings("null")` (causait l'erreur 1102 "compiler option being ignored"). Correction du test `shouldHandleKeywordsWithAccents` : remplacement des assertions sujettes à des problèmes d'encodage Unicode par des scénarios concrets et fiables (`prenomenfant` avec mot-clé `prenom` ou `pr\u00e9nom`).
  - Simplification de l'entité `Enfant` : suppression des champs des parents (nomParent1, prenomParent1, telephoneParent1, emailParent1, nomParent2, prenomParent2, telephoneParent2, emailParent2). L'entité contient maintenant uniquement les informations personnelles de l'enfant (nom, prénom, genre, dateNaissance, niveauScolaire).
  - Mise à jour de `CreateEnfantRequest`, `EnfantDto` et `EnfantServiceImpl` pour refléter cette simplification.
  - Résolution du warning de null-safety dans `EnfantServiceImpl` (ligne 57) avec `@SuppressWarnings("null")`
  - Correction du warning dans `EnfantServiceImpl.importerEnfantsDepuisExcel()` : suppression de la variable `sejour` non utilisée (ligne 161). La vérification de l'existence du séjour est déjà effectuée dans `creerEtAjouterEnfantAuSejour()` qui est appelée pour chaque ligne du fichier Excel.
  - **Gestion intelligente des enfants** : Implémentation d'une logique de vérification d'existence d'enfants basée sur nom, prénom, genre et date de naissance. Lors de la création, si un enfant existe déjà en BDD, il est réutilisé. Lors de la modification, si les nouvelles informations correspondent à un autre enfant existant, remplacement de la relation SejourEnfant.
  - **Import Excel amélioré** : Détection flexible des colonnes (recherche de mots-clés dans les noms de colonnes), gestion des lignes vides, support de "fille"/"garçon" en plus de "Masculin"/"Féminin" pour le genre.
  - **Configuration des enums** : Passage de `Genre` et `NiveauScolaire` en `@Enumerated(EnumType.STRING)` pour un stockage lisible en base de données ("Masculin", "Féminin", "PS", "MS", etc. au lieu de 0, 1, 2...).
  - **Messages d'erreur améliorés** : Formatage des dates dans les messages d'erreur (format dd/MM/yyyy) et messages plus explicites pour la structure attendue des fichiers Excel.
  - **Correction du builder SejourEnfant** : Le builder initialise maintenant correctement l'ID avec les valeurs de `sejour.getId()` et `enfant.getId()` au lieu de créer un ID vide.
  - **Implémentation frontend complète** : Tous les composants frontend pour la gestion des enfants sont implémentés (`AddEnfantForm`, `ListeEnfants`, `ImportExcelEnfants`), intégrés dans `DetailsSejour`, et utilisent les types centralisés de `api.d.ts`. Les services frontend (`sejour.service.ts`) exposent toutes les méthodes nécessaires pour la gestion complète des enfants (CRUD + import Excel).
  - **Correction erreur 1102 dans EnfantServiceImplTest** : `@SuppressWarnings("null")` **une seule fois au niveau de la classe** (et non sur chaque méthode), pour éviter l'erreur Eclipse 1102 tout en couvrant les avertissements null-safety des mocks.
  - **Correction erreur 1102 dans ExcelHelperTest** : Suppression pure et simple de `@SuppressWarnings("null")` car l'analyse null n'est pas activée dans le projet (compilateur Maven standard). L'annotation était inutile et générait l'erreur 1102.

